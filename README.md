# CNN-Model-to-Detect-Cats
This is a little fun passion project where I make a CNN to detect a cat that I've been taking care of. The model will be trained with PyTorch and exported to OpenCV so that it runs on a Raspberry Pi 3 Model B.

Images files are stored in an ignored /my-cat and /not-my-cat directories.
